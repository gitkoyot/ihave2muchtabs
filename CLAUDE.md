# Project Rules

## Version Bumping

Every time any change is made to the extension code, bump the revision (patch) version in both `extension/manifest.json` and `extension/package.json` before committing.
